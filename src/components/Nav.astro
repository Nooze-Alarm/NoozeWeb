---
const { active } = Astro.props;
const links = [
    { href: 'index.html', label: 'Home', key: 'home' },
    { href: 'features.html', label: 'Features', key: 'features' },
    { href: 'about.html', label: 'About', key: 'about' },
    { href: 'faq.html', label: 'FAQ', key: 'faq' },
    { href: 'contact.html', label: 'Contact', key: 'contact' },
];
---
<nav class="nav-blur fixed top-0 w-full z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-[1fr_auto_1fr] items-center h-16 gap-4">
            <a href="index.html" class="nav-brand flex items-center gap-3 text-xl font-bold text-white tracking-tight justify-self-start">
                <img src="/assets/logo.png" alt="Nooze" width="34" height="34" class="w-[34px] h-[34px] rounded-lg" />
                Nooze&nbsp;<span class="accent">Alarm</span>
            </a>

            <div class="hidden md:flex items-center gap-8 justify-self-center col-start-2">
                {links.map((l) => (
                    <a href={l.href} class={l.key === active ? 'nav-link active' : 'nav-link'}>{l.label}</a>
                ))}
            </div>

            <div class="flex items-center gap-3 justify-self-end col-start-3">
                <a href="support.html" class={active === 'support' ? 'nav-pill active hidden md:inline-flex' : 'nav-pill hidden md:inline-flex'}>Support Us</a>
                <button id="mobileMenuBtn" class="md:hidden text-gray-300 hover:text-primary p-2" aria-label="Toggle menu">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
                    </svg>
                </button>
            </div>
        </div>

        <div id="mobileMenu" class="hidden md:hidden pb-4">
            <div class="flex flex-col gap-3 pt-2">
                {links.map((l) => (
                    <a href={l.href} class={l.key === active ? 'text-primary font-medium py-2' : 'text-gray-300 hover:text-primary py-2 transition-colors'}>{l.label}</a>
                ))}
                <a href="support.html" class={active === 'support' ? 'text-primary font-medium py-2' : 'text-gray-300 hover:text-primary py-2 transition-colors'}>Support Us</a>
            </div>
        </div>
    </div>
</nav>
